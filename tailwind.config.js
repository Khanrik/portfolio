// tailwind.config.js
module.exports = {
    theme: {
        extend: {
            colors: {
                mainBackground: 'rgba(var(--mainBackground))',
                secondBackground: 'rgba(var(--secondBackground))',
                bodyText: 'rgba(var(--bodyText))',
                headerText: 'rgba(var(--headerText))',
                mainTitleSecond: 'rgba(var(--mainTitleSecond))',
                cardBackground: 'rgba(var(--cardBackground))',
            },
        },
    },
    variants: {},
    plugins: [],
};